server:
  port: 8080

spring:
  application:
    name: app
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      timeout: 3s
  mail:
    host: smtp.gmail.com
    port: 587
    username: dnatestingnotification@gmail.com
    password: ugltgfcsfppohwam
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: ${DBMS_CONNECTION:jdbc:mysql://localhost:3306/intelligent_resume_system}
    username: ${DBMS_USERNAME:app_user}
    password: ${DBMS_PASSWORD:app_password}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  springdoc:
    swagger-ui:
      path: /docs
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    springframework.web: DEBUG
    springframework.web.socket: DEBUG
    springframework.security: DEBUG

app:
  jwt:
    secret-key: ${JWT_SECRET_KEY:cb3f30b0ec715f1bcbead50dc9d77786c038b569f2f6adb9a9e724e07ffd6c17}
    token-exp: PT15M # 15 minutes
    refresh-token-exp: P1D # 1 day

mail:
  sender:
    from: ${MAIL_SENDER_FROM:dnatestingnotification@gmail.com}
